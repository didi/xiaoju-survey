# 任务拆解：新增图片上传题

- **对应需求：** `work/requests/2026-03-31-图片上传题-prd.md`
- **对应方案：** `work/specs/2026-03-31-图片上传题-spec.md`
- **对应计划：** `harness/plans/active/2026-03-31-图片上传题.md`
- **拆解日期：** 2026-03-31

---

## 任务列表

### 阶段一：枚举扩展（前后端各增一个新类型）

| # | 任务 | 依赖 | 验收条件 | 状态 |
|---|------|------|---------|------|
| 1 | 后端枚举：在 `server/src/enums/question.ts` 新增 `IMAGE_UPLOAD = 'image-upload'` | 无 | `grep "IMAGE_UPLOAD" server/src/enums/question.ts` 有输出 | done |
| 2 | 前端枚举：在 `web/src/common/typeEnum.ts` 新增 `IMAGE_UPLOAD`、typeTagLabels、ADVANCED 数组追加 | 无 | `grep "image-upload" web/src/common/typeEnum.ts` 有输出 | done |

### 阶段二：Widget 实现（B 端预览 + C 端交互）

| # | 任务 | 依赖 | 验收条件 | 状态 |
|---|------|------|---------|------|
| 3 | 新建 `web/src/materials/questions/widgets/ImageUploadModule/meta.js`：定义 field/title/type/isRequired/showIndex/showType/showSpliter/maxImageCount/maxFileSize，formConfig 包含 basicConfig + 上传限制自定义区 | #2 | 文件存在；`maxImageCount` 默认值为 5，`maxFileSize` 默认值为 100 | done |
| 4 | 新建 `web/src/materials/questions/widgets/ImageUploadModule/index.jsx`：<br>- B 端只读预览（展示占位图 + 提示文案）<br>- C 端：`<input type="file" multiple accept>` + 上传逻辑（格式/大小校验、调用 `/api/file/upload`、并发控制、上传中取消、删除）<br>- `@change` emit URL 数组作为 value | #3 | B 端搭建页可见组件预览；C 端能上传/删除图片并 emit 正确 value 格式 | done |

### 阶段三：题型三处注册（INV-02）

| # | 任务 | 依赖 | 验收条件 | 状态 |
|---|------|------|---------|------|
| 5 | `web/src/materials/questions/common/config/moduleList.js` 新增 `'image-upload': 'ImageUploadModule'` | #3 #4 | `grep "image-upload" web/src/materials/questions/common/config/moduleList.js` 有输出 | done |
| 6 | `web/src/management/config/questionMenuConfig.js`：menuItems 新增 image-upload 条目（type/path/snapshot/icon/title），高级题型 questionList 追加 `'image-upload'` | #5 | B 端题型菜单"高级题型"分组中出现"图片上传"选项 | done |

### 阶段四：数据分析展示

| # | 任务 | 依赖 | 验收条件 | 状态 |
|---|------|------|---------|------|
| 7 | `server/src/modules/survey/services/dataStatistic.service.ts`：在 listBody 构建循环中新增对 `QUESTION_TYPE.IMAGE_UPLOAD` 的处理，将 URL 数组转换为 `<img>` 标签字符串 | #1 | 数据列表页对图片上传题显示缩略图而非原始 URL | done |
| 8 | `web/src/management/pages/analysis/components/DataTable.vue`：调整 `.table-row-cell :deep(img)` 样式，使图片缩略图宽高合适（约 60px），或新增图片展示 layout | #7 | 分析页数据列表中图片缩略图正常展示，不溢出单元格 | done |

---

## 状态说明

- `pending` — 未开始
- `in_progress` — 进行中
- `done` — 已完成并验收
- `blocked` — 被阻塞（注明原因）
- `skipped` — 跳过（注明原因）

---

## 阻塞记录

| 任务 # | 阻塞原因 | 解决方案 | 状态 |
|--------|---------|---------|------|
| #6 | `snapshot` 图片 `/imgs/question-type-snapshot/image-upload.webp` 尚无设计稿 | 临时复用 cascader.webp 占位，后续替换 | 待确认 |
| 全部 | C 端上传 `channel` 参数依赖生产环境配置 | 本地开发用 LocalHandler 可直接跑，生产环境部署前需确认 channel 名 | 待确认 |

---

## 完成检查

任务全部 done 后，按顺序执行：

### 第一层：静态检查

```bash
cd server && npm run lint && npm run test
cd web && npm run type-check && npm run lint && npm run build
```

- [x] server lint: PASS
- [x] server test: PASS (61 suites, 353 tests)
- [x] web type-check: PASS
- [x] web lint: PASS (0 errors, 19 pre-existing warnings)
- [x] web build: PASS

### 第二层：不变量检查（INV-02）

```bash
# 三处注册数量核对
ls web/src/materials/questions/widgets/ | wc -l
grep -c "type:" web/src/management/config/questionMenuConfig.js
grep -c "'" web/src/materials/questions/common/config/moduleList.js
```

- [x] INV-02 三处注册一致（widgets/ImageUploadModule + menuConfig 'image-upload' 条目 + moduleList 'image-upload' 映射）
- [x] INV-04 MPA 隔离：ImageUploadModule 放在 materials/questions/widgets/，无跨入口引用
- [x] INV-05 无硬编码：channel 使用字符串常量 'XIAOJU_SURVEY_UPLOAD'（与 spec 规格一致），无 OSS 密钥硬编码

### 第三层：核心链路验证

变更涉及"题型物料"域 → 必须验证：**路径二（新题型端到端）** + **路径一（问卷全生命周期）**

```
路径二（新题型端到端）验证步骤：
1. B 端：新建问卷，从"高级题型"菜单添加"图片上传"题
2. B 端：配置题目（修改标题、设置最大5张、单文件限制10MB）
3. 发布问卷
4. C 端：打开问卷链接，上传 ≤5 张格式合法的图片
5. C 端：验证超限提示（上传第6张）、格式拦截（上传非图片文件）
6. C 端：提交问卷
7. B 端数据分析：查看数据列表，确认图片缩略图展示
8. B 端分题统计：确认图片上传题不出现
```

- [ ] 路径二：PASS / FAIL / 需手动确认
- [ ] 路径一（回归）：问卷全生命周期其他题型未受影响

### 第四层：验收条件逐条

见 `work/requests/2026-03-31-图片上传题-prd.md` § 验收条件

---

## 蒸馏提示

> 任务完成后检查：
> - 新增题型的完整注册步骤（包含 meta.js 字段设计经验）→ `harness/docs/engineering/component-library.md`
> - 文件上传 channel 配置方式 → `harness/docs/engineering/backend-guidelines.md`
> - dataStatistic 按类型扩展的模式 → `harness/docs/engineering/backend-guidelines.md`
> - 如遇任何新坑 → `harness/docs/engineering/pitfalls.md`
